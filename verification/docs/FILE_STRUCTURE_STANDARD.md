# MAZE项目验证环境文件结构规范

## 📋 文档概述

本文档定义了MAZE网络验证环境的标准化文件结构，**任何时候都必须强制执行此规范**。

## 🏗️ 标准目录结构

```
verification/                    # 验证环境根目录
├── README.md                   # 验证环境总览文档
├── Makefile                    # 主构建和测试入口
├── CLAUDE_VERIFICATION.md      # 验证环境Claude使用指南
├── docs/                       # 📚 验证文档
│   ├── architecture/           # 架构设计文档
│   ├── test_plans/            # 测试计划和规格
│   └── user_guides/           # 用户使用指南
├── rtl/                        # 📄 RTL源代码（从项目根目录链接）
│   ├── include/               # 接口和全局定义
│   ├── src/                   # RTL源文件
│   └── lib/                   # 第三方IP库
├── testbench/                  # 🧪 测试台代码
│   ├── unit_tests/            # 单元测试
│   ├── integration_tests/     # 集成测试
│   ├── system_tests/          # 系统测试
│   ├── utils/                 # 测试工具和库
│   └── README.md              # 测试台使用说明
├── scripts/                    # 🔧 自动化脚本
│   ├── build/                 # 构建脚本
│   ├── run/                   # 运行脚本
│   ├── analysis/              # 结果分析脚本
│   └── verification/          # 规范检查脚本
├── sim/                        # 🗂️ 仿真运行环境
│   ├── work/                  # 临时工作目录（git忽略）
│   └── waves/                 # 波形文件存储
├── reports/                    # 📊 验证报告和结果
│   ├── simulation/            # 仿真结果
│   ├── coverage/              # 覆盖率报告
│   ├── performance/           # 性能分析
│   └── regression/            # 回归测试结果
└── logs/                       # 📋 运行日志
    ├── simulation/            # 仿真日志
    ├── error/                 # 错误日志
    └── regression/            # 回归日志
```

## 📂 目录详细规范

### 📚 `docs/` - 验证文档
```
docs/
├── architecture/
│   ├── testbench_design.md    # 测试台架构设计
│   └── verification_methodology.md
├── test_plans/
│   ├── test_point_decomposition.md    # 测试点分解
│   ├── test_cases_specification.md    # 测试用例规格
│   └── verification_plan.md           # 验证计划
└── user_guides/
    ├── quick_start_guide.md           # 快速开始指南
    └── test_execution_guide.md        # 测试执行指南
```

**职责**: 存储所有验证相关的设计文档、测试计划和用户指南。

### 🧪 `testbench/` - 测试台代码
```
testbench/
├── unit_tests/                # 单元测试（模块级）
│   ├── node/                  # 节点模块测试
│   ├── topo/                  # 拓扑模块测试
│   ├── arbiter/               # 仲裁器测试
│   └── router_unit/           # 路由单元测试
├── integration_tests/         # 集成测试（多模块）
│   ├── node_basic_routing/    # 节点基本路由测试
│   ├── network_topology/      # 网络拓扑测试
│   └── fault_tolerance/       # 故障容错测试
├── system_tests/              # 系统测试（完整网络）
│   ├── full_network/          # 完整64节点网络测试
│   ├── performance/           # 性能和压力测试
│   └── stress/                # 压力测试
├── utils/                     # 测试工具和库
│   ├── packet_generator.h     # 数据包生成器
│   ├── result_checker.h       # 结果检查器
│   ├── waveform_utils.svh     # 波形分析工具
│   └── coverage_utils.svh     # 覆盖率分析工具
└── README.md                  # 测试台使用说明
```

**职责**: 存储所有SystemVerilog和C++测试代码。

**命名规范**:
- SystemVerilog文件: `*.sv` 或 `*.svh`
- C++测试文件: `test_*.cpp`
- 头文件: `*.h` 或 `*.svh`
- 目录名使用小写字母和下划线

### 🔧 `scripts/` - 自动化脚本
```
scripts/
├── build/                     # 构建脚本
│   ├── build_unit_test.sh     # 单元测试构建
│   ├── build_integration_test.sh  # 集成测试构建
│   └── build_system_test.sh   # 系统测试构建
├── run/                       # 运行脚本
│   ├── run_unit_test.sh       # 运行单元测试
│   ├── run_integration_test.sh  # 运行集成测试
│   └── run_regression.sh      # 运行回归测试
├── analysis/                  # 结果分析脚本
│   ├── collect_coverage.sh    # 覆盖率收集
│   ├── analyze_performance.sh # 性能分析
│   └── generate_report.py     # 报告生成
└── verification/              # 规范检查脚本
    ├── check_file_structure.sh   # 文件结构检查
    ├── validate_naming.sh     # 命名规范检查
    └── enforce_standards.py   # 标准强制执行
```

**职责**: 存储所有自动化脚本和工具。

**命名规范**:
- 脚本文件: `动词_名词.sh` (如 `build_unit_test.sh`)
- Python脚本: `名词_动词.py` (如 `report_generate.py`)
- 所有脚本必须有可执行权限

### 🗂️ `sim/` - 仿真运行环境
```
sim/
├── work/                      # 临时工作目录（git忽略）
│   ├── build/                 # 编译产物
│   └── run/                   # 运行时临时文件
└── waves/                     # 波形文件存储
    ├── vcd/                   # VCD格式波形
    ├── fst/                   # FST格式波形
    ├── fsdb/                  # FSDB格式波形
    └── vpd/                   # VPD格式波形
```

**职责**: 仿真运行时的临时文件和波形存储。

**关键规则**:
- `work/` 目录下所有内容都被 `.gitignore` 忽略
- 每次运行前应清理 `work/` 目录
- 波形文件按格式分类存储

### 📊 `reports/` - 验证报告
```
reports/
├── simulation/                # 仿真执行报告
│   ├── unit_test_results/     # 单元测试结果
│   ├── integration_test_results/  # 集成测试结果
│   └── system_test_results/   # 系统测试结果
├── coverage/                  # 覆盖率报告
│   ├── code_coverage/         # 代码覆盖率
│   ├── functional_coverage/   # 功能覆盖率
│   └── toggle_coverage/       # 翻转覆盖率
├── performance/               # 性能分析报告
│   ├── throughput_analysis/   # 吞吐量分析
│   └── latency_analysis/      # 延迟分析
└── regression/                # 回归测试结果
    ├── daily/                 # 每日回归
    └── weekly/                # 每周回归
```

**职责**: 存储所有验证报告和分析结果。

### 📋 `logs/` - 运行日志
```
logs/
├── simulation/                # 仿真执行日志
│   ├── YYYYMMDD_unit_test.log
│   ├── YYYYMMDD_integration_test.log
│   └── YYYYMMDD_system_test.log
├── error/                     # 错误日志
│   ├── compilation_errors.log
│   └── runtime_errors.log
└── regression/                # 回归测试日志
    └── YYYYMMDD_regression.log
```

**职责**: 存储所有运行时日志。

**命名规范**:
- 日志文件: `YYYYMMDD_类型.log`
- 错误日志按错误类型分类

## 🚫 强制规则

### 文件放置规则
1. **严禁**在根目录放置测试代码
2. **严禁**在 `sim/` 目录放置源代码
3. **严禁**在 `work/` 目录提交任何文件到git
4. **必须**按功能模块分类放置测试文件

### 命名规则
1. 目录名: 小写字母 + 下划线
2. 文件名: 小写字母 + 下划线 + 点号分隔后缀
3. SystemVerilog: `*.sv` (设计) / `*.svh` (包含)
4. C++文件: `test_*.cpp` (测试) / `*.h` (头文件)
5. 脚本文件: `动词_名词.sh`

### Git管理规则
1. 所有临时文件和目录必须加入 `.gitignore`
2. 波形文件可以提交但建议压缩
3. 报告文件按日期归档
4. 删除超过30天的临时日志

## 🔍 规范检查

### 自动检查脚本
运行 `scripts/verification/check_file_structure.sh` 验证文件结构合规性。

### 手动检查清单
- [ ] 所有源文件都在正确目录下
- [ ] 临时文件都被git忽略
- [ ] 文件命名符合规范
- [ ] 目录结构无重复
- [ ] README文件完整

## 🛠️ 迁移指南

### 从旧结构迁移
1. 将 `testbench/node_basic_routing/` 移至 `testbench/integration_tests/node_basic_routing/`
2. 将 `unit_tests/` 移至 `testbench/unit_tests/`
3. 清理 `sim/build/` 中的临时文件
4. 重新组织脚本到 `scripts/` 对应子目录

### 清理操作
```bash
# 清理临时文件
rm -rf sim/work/
rm -rf verification/

# 重新组织文件
mkdir -p testbench/integration_tests/
mv testbench/node_basic_routing testbench/integration_tests/
```

---

**⚠️ 重要提醒**: 此规范为强制性文档，任何对验证环境的修改都必须严格遵守此文件结构！**